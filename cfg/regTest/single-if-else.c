#include <stdio.h>
#include <stdlib.h>
/* Test file for single if/else statement in main*/

int main(int argc, char **argv){
  int test = 10;
  if(test == 10){
    return(0);
  } else {
    return(1);
  }
}

